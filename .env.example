# Azure AD Configuration
# Replace these values with your actual Sanofi Azure AD settings

# Your Azure App Registration Client ID
VITE_AZURE_CLIENT_ID=your-azure-app-client-id

# Sanofi Azure AD Tenant ID
VITE_AZURE_AUTHORITY=https://login.microsoftonline.com/your-sanofi-tenant-id

# Redirect URI (usually your app's URL)
VITE_REDIRECT_URI=http://localhost:5173

# Production Settings
# VITE_AZURE_CLIENT_ID=production-client-id
# VITE_AZURE_AUTHORITY=https://login.microsoftonline.com/sanofi-tenant-id
# VITE_REDIRECT_URI=https://your-production-domain.com

# Power BI Configuration
# These values are exposed to the browser, so use VITE_ prefix
VITE_POWERBI_WORKSPACE_ID=your-workspace-id
VITE_POWERBI_DATASET_ID=your-dataset-id

# Server-side AI settings (used by Vercel functions in /api)
# These are NOT exposed to the browser and must not use VITE_
OPENAI_API_KEY=your-openai-key
# Optional: override model or base URL
CHAT_MODEL=gpt-4o-mini
OPENAI_BASE=https://api.openai.com/v1
